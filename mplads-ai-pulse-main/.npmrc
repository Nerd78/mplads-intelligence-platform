; react-simple-maps@3 declares peer react 16/17/18 and has no React 19 release.
; It works fine at runtime here, so installs resolve with legacy peer rules
; instead of every contributor needing to remember --legacy-peer-deps.
legacy-peer-deps=true
